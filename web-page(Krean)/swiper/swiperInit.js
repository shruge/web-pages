const swiper_references = new Swiper('.swiper-references', {
	loop: true,
	speed: 600,
	grabCursor: true,
	autoHeight: true,

	navigation: {
		prevEl: '.swiper-button-prev',
		nextEl: '.swiper-button-next',
	},

})